# Optional defaults for run.sh.
# Leave IMAGE/DEVICE blank to be prompted at runtime.

# IMAGE is resolved relative to run.sh unless absolute.
IMAGE="2025-12-04-raspios-trixie-arm64-lite.img.xz"
# Optional download URL used when IMAGE (.xz) is missing locally.
IMAGE_URL="https://downloads.raspberrypi.com/raspios_lite_arm64/images/raspios_lite_arm64-2025-12-04/2025-12-04-raspios-trixie-arm64-lite.img.xz"
# DEVICE="/dev/sdb"

# Optional explicit boot partition if auto-detection is wrong.
# BOOT_PARTITION="/dev/sdb1"
